apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "azure-vote.fullname" . }}-config
  labels:
    {{- include "azure-vote.labels" . | nindent 4 }}
data:
  config_file.cfg: |
{{ .Values.config.data.config_file.cfg | indent 4 }}
