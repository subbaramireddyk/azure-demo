apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "azure-vote.fullname" . }}-sa
  namespace: {{ .Release.Namespace }}
  annotations:
    azure.workload.identity/client-id: "{{ .Values.workloadIdentityClientId }}"
  labels:
    azure.workload.identity/use: "true"